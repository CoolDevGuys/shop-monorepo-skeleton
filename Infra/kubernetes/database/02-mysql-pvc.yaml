apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv-volume
  namespace: shop
  labels:
    type: local
    app: shop-db
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
  namespace: shop
  labels:
    app: shop-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
